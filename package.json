{
  "name": "stlapps",
  "private": true,
  "description": "StuntListing Apps — one launcher for every StuntListing app and stunt tool.",
  "scripts": {
    "start": "npx --yes serve public -l 4173",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:preview": "node scripts/build-preview.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
